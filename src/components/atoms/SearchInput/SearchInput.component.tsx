import React from 'react';

import { Container, Icon, Input } from './assets/styles';

const SearchInput: React.FC = () => {
  return (
    <Container>
      <Icon />
      <Input />
    </Container>
  );
};

export default SearchInput;
